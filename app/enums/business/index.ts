export enum SensorStatus {
  OPERATING = 'operating',
  ALERT = 'alert',
}

export enum SensorType {
  ENERGY = 'energy',
}

export enum TreeElementType {
  LOCATION = 'location',
  ASSET = 'asset',
  COMPONENT = 'component',
}
