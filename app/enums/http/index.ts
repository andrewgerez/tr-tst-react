export enum Endpoints {
  /**
   * Endpoint to get the list of companies
   */
  GET_COMPANIES = 'companies',

  /**
   * Endpoint to get the locations of a specific company
   */
  GET_LOCATIONS = 'locations',

  /**
   * Endpoint to get the assets of a specific company
   */
  GET_ASSETS = 'assets',
}
